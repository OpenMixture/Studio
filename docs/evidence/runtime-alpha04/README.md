# Exact runtime candidate upgrade — 2026-09-20

English | [简体中文](./README.zh-CN.md)

**The candidate-archive upgrade gates pass.** This closes Studio's ALPHA-03 candidate-consumption dependency for engine ALPHA-04. These IDs belong to separate repository plans. Exact registry-version consumption, publication, redeployment and human trial results remain pending; the existing Pages trial still uses its recorded historical runtime.

Clean Studio executable revision `6b2d53e3de16b21725b2a4359a2263f98671a6f9` installs the clean engine `82b74707b2a8a998190e2f28b16f91fb9614486a` archive. Version remains unpublished `0.1.0-alpha.0`, build ID `sha256:94f9cc455fcd5f805942b0196b39a276782ce0e504772c3a140ea8ad85814a28`, SHA-256 `a9bcfe8d849f9fb9982a750dd99d026807fdf6453b5be491deeda90e99a2c6ae`. [Producer](./producer.json) and [actual build probe](./build-probe.json) bind the delivered bytes; historical-WASM mixing is rejected. Only the vendor archive, build receipt and runtime lock integrity changed. Later documentation commits are not the executable revision.

The engine material CI already qualified these exact bytes in [run 35489430243](https://github.com/OpenMixture/OpenMixture/actions/runs/35489430243). This product then freshly ran `npm ci`, `npm run check` (20 unit tests, public types and production build), 52 controlled browser tests, normal deployment, actual Studio download capture, native preparation and independent Player PNG export. Both Windows and filesystem-isolated Linux/WSL executions passed all seven 1K cases / 28 channels under frozen v2 rules. Each has 12 exact channels; the remaining 16 differ by at most one component value, with maximum local bias 0.0078125. Exact checker rules, plans, material structures, causality and height/normal relationships pass. No thresholds, runtime sources, fixtures or native goldens changed during this upgrade.

- Windows: Chromium 153.0.8010.12 with recorded test flags, Windows 11 build 26100. [52 contracts](./windows-browser-summary.json), [deployment](./windows-deployment.json), [Player identity](./windows-player.json), [full comparison](./windows-comparison.json).
- Ordinary Chrome 153.0.8010.48: [receipt](./ordinary.json) verifies only profile/CDP/blank-page launch arguments, new/edit/repair/undo/redo/save/reopen, Studio and independent Player four-channel PNG bytes/encodings, disposal and a separate injected GPU-unavailable CPU-editing probe.
- Linux/WSL2: [isolation](./isolation.json) denies both source checkouts and exposes no Rust tools; npm installation remains network-enabled. Chromium 153.0.8010.12 uses explicit SwiftShader test flags. [52 contracts](./linux-browser-summary.json), [deployment](./linux-deployment.json), [Player identity](./linux-player.json), [full comparison](./linux-comparison.json). This is not ordinary Linux browser support.

Both environments use Node 24.20.0, npm 11.19.0 and Playwright 1.63.0. Native comparison uses the matching clean engine's debug CLI on explicit DX12 / NVIDIA GeForce GT 1030; browser-redacted adapter identity remains redacted. Raw Windows/Linux lock digests differ by line endings; their complete parsed dependency graphs and archive integrity agree.

The producer owns the [full durable delivery evidence](https://github.com/OpenMixture/OpenMixture/blob/9521ca307af0a405059806fc1c0177ba4e8ade5b/docs/evidence/alpha-04/README.md), including archive, all saved bytes/native references/Windows/Linux PNGs, contact sheets, hashes, restoration verifier, reproduction commands and initial local environment failures. This product retains compact receipts and its intentional vendor archive; routine full logs and compiler products remain temporary. The [previous failed candidate](../alpha-candidate/README.md) is preserved as its original result and is superseded for delivery by this new candidate, not rewritten as a pass. No engine source import, Rust install dependency, new renderer or M6 scope was introduced.
