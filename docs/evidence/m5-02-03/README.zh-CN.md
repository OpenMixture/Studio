# M5-02／M5-03 本地验收——2026-09-14

[English](./README.md) | 简体中文

**通过：初始浏览器执行与隔离打包消费。** 干净产品提交 `d38bf68ddc470a33c608592363e3554f05887fe1` 的 13 项 Chromium 测试通过，零失败／跳过。后续证据提交不是已测源码。运行时归档未变更：引擎 `4b914feb9f3365d292b27ea60c5e0b6004f745e8`，版本 `0.1.0-alpha.0`，SHA-256 `9e245578de160cee1050259ef34358c3fcd3353a21bbb1672d29701bc1ac8084`。[摘要](./summary.json)绑定源码、锁、夹具、归档及保留附件。

隔离运行于 2026-09-14 04:33 UTC，使用 macOS 26.5.1 arm64、Node 24.20.0、npm 11.19.0、Chromium 153.0.8010.12。沿用显式 `--enable-unsafe-webgpu` 与 `--ignore-gpu-blocklist` 参数，服务路径为 `/player/`。[运行时上下文](./runtime-context.json)记录实际浏览器、请求策略、限制及被隐去的 `BrowserWebGpu` 适配器字段。不从浏览器推断硬件身份。

| 门槛 | 保留证据 |
|---|---|
| 软件包隔离 | [回执](./isolation.json)与[负向探针](./isolation-probe.txt)：macOS 沙箱拒绝读取原引擎／产品检出，子进程 PATH 中无 cargo／rustc。全新 `npm ci`、类型／生产检查和浏览器执行均退出零。 |
| 公开 API／包身份 | [测试结果](./browser-results.json)：确切构建信息与 vendor 来源一致；整数投影符合声明；CPU 方法无需 GPU；包内 WASM 在非根路径使用默认 URL、显式 URL 和字节均正常。资源缺失仍返回结构化失败。 |
| 真实像素与所有权 | 保留精确 65 × 3 棋盘格字节、后续渲染及销毁、请求捕获、忙状态拒绝与可见 Player 消费覆盖。 |
| 设备丢失 | [两次渲染之间](./loss-between-renders.json)与[映射期间](./loss-during-map.json)：真实设备销毁交付丢失；首个读回失败保留 `MIX_READBACK_FAILED`；后续调用报告 `MIX_GPU_DEVICE_LOST`；跟踪的实时分配字节为零；已返回像素仍有效，显式创建的新设备可以渲染。 |
| 失败清理 | [映射验证失败](./map-failure.json)：真实不对齐映射被拒绝；读回解除映射与错误作用域平衡；后续渲染及失败渲染期间的销毁均结束；没有未捕获 GPU 错误或未处理页面失败。 |
| 重复执行 | [八个模块／设备与 32 次渲染](./repeated-lifecycle.json)：每次调用跟踪的实时字节归零、分配字节已释放、每设备一个棋盘格流水线、八次显式销毁、首次输出保留。 |
| 获取错误分类 | API 不可用、空适配器与设备获取拒绝是明确注入的分类案例，与真实 GPU 执行区分。 |

通过[已提交步骤](../../m5-02-03.zh-CN.md)复现。完整 vendor 归档、测试源码、精简结果与附件保留在 Git；原临时检出、完整日志和原始 Playwright 报告不承诺永久保留。复现创建新的运行。产品视觉与像素未改变，因此不新增截图；[初始预览](../browser-start/README.zh-CN.md)继续作为历史证据。

本记录接受此环境内有界的 M5-02／M5-03 检查点。`GPUDevice.destroy()` 是可控的真实设备丢失，不代表自发硬件／驱动故障、自动恢复或标签页终止覆盖。分配描述符不是实测物理 GPU 内存。三材质 1K 回归、更广压力／兼容性、正式浏览器 CI 矩阵及完整 Player 参数／导出／调度仍属于 M5-04／M5-05。npm 发布、网站部署与 Studio 编辑不在本次范围内。GitHub 集成／检查结果与此次本地运行分别记录。
